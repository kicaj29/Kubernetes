apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "FancyMicroserviceHelmPackage.fullname" . }}-test-connection"
  labels:
    {{- include "FancyMicroserviceHelmPackage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "FancyMicroserviceHelmPackage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
