apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-metrics-service-helm-package.fullname" . }}
  labels:
    {{- include "custom-metrics-service-helm-package.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "custom-metrics-service-helm-package.selectorLabels" . | nindent 4 }}
