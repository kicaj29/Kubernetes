apiVersion: v1
kind: Service
metadata:
  name: {{ include "fancy-micro-service-helm-package.fullname" . }}
  labels:
    {{- include "fancy-micro-service-helm-package.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fancy-micro-service-helm-package.selectorLabels" . | nindent 4 }}
