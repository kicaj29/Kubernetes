apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fancy-micro-service-helm-package.fullname" . }}-test-connection"
  labels:
    {{- include "fancy-micro-service-helm-package.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fancy-micro-service-helm-package.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
